/**
 * 1️⃣ **Verificando o tamanho da string**

- Declare uma variável `frase` com a string `"JavaScript é incrível!"`.
- Use a propriedade `.length` para exibir no console o número de caracteres dessa string.
 */
// Declare a variável 'frase' com a string desejada
let frase = "JavaScript é incrível!";
// Use a propriedade .length para obter o número de caracteres e exiba no console
console.log(frase.length);
